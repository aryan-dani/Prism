# Prism frontend

React + TypeScript + Vite UI for the Prism chat agent.

```powershell
npm install
npm run dev
```

Expects the FastAPI backend at `http://127.0.0.1:8000` (override with `VITE_API_BASE`).

See the repo root [README](../README.md) for full setup.
